This application represents a lightweight Node.js service containerized using Docker.

The purpose of keeping the application simple is to focus on DevOps automation rather than application complexity.

The container image is built during the CI pipeline and deployed to Kubernetes.