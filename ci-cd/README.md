This CI/CD pipeline is implemented using GitHub Actions.

It performs container build and integrates vulnerability scanning using Trivy before deployment.

The pipeline is triggered automatically on every push to the main branch, reflecting continuous integration practices used in enterprise environments.