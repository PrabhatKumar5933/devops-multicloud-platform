The Kubernetes deployment is configured with two replicas to ensure high availability.

Rolling updates are enabled by default in Kubernetes deployments to prevent downtime during version upgrades.

Horizontal Pod Autoscaler scales the application automatically when CPU utilization exceeds 70 percent, ensuring performance stability under load.